# Analytical package

Stage 1 is intentionally deterministic. The package provides ingestion, profiling, quality checks, descriptive analysis, a read-only DuckDB query layer, and structured result objects.

The AI agent will be layered on top of these capabilities in Stage 2 rather than replacing them.
